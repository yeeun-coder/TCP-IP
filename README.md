# TCP-IP
## mac os 터미널

* 만들기
  vi (파일 이름).c

* 컴파일
  gcc (파일 이름).c -o (이름)
  - 파일 수정할 때마다 컴파일 해줘야 한다.

* 실행
  ./(이름)

* 복사
  cp (복사할 파일 이름).c (파일 이름).c


* 간편한 기능
  :set number -> vi 해서 라인수 볼 수 있게 해준다.

ESC yy
ESC P
